<form method="post"  class="uk-grid uk-form-horizontal" uk-grid>

    <div class=" uk-width-1">
        <label class="uk-form-label required">Benutzername</label>
        <input type="text" name="username" class="uk-input" required="required" value="{{ user.username|default('') }}">
    </div>

    <div class="uk-width-1">
        <label class="uk-form-label required">E-Mail-Adresse</label>
        <input type="email" name="email" class="uk-input" required="required" value="{{ user.email|default('') }}">
    </div>

    <div class="uk-width-1-2@m">
        <label class="uk-form-label required">Passwort</label>
        <input type="password" name="password" class="uk-input" required="required" value="{{ user.password|default('') }}">
    </div>
    <div class="uk-width-1-2@m">
        <label class="uk-form-label required">Passwort wiederholen</label>
        <input type="password" name="password_check" class="uk-input" required="required" value="{{ user.password_check|default('')}}">
    </div>

    <div class="uk-width-1">
        <label class="uk-form-label required">Vorname</label>
        <input type="text" name="firstname" class="uk-input" required="required" value="{{ user.firstname|default('') }}">
    </div>

    <div class="uk-width-1">
        <label class="uk-form-label required">Nachname</label>
        <input type="text" name="lastname" class="uk-input" required="required" value="{{ user.lastname|default('') }}">
    </div>

    <div class="uk-width-1">
        <input type="hidden" name="csrf_token" value="{{ app.csrf_token }}">
        <button type="submit" class="uk-button uk-button-primary">{{ button|default('Registrieren') }}</button>
    </div>

</form>